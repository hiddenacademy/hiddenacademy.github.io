
```html
---
layout: post
title: "Introduction au Shell Linux - Les Fondamentaux"
date: 2025-06-26
categories: tutorial
image: /assets/images/linux-terminal.png
comments: 3
---

# Introduction au Shell Linux

## Qu'est-ce qu'un Shell ?

Le **shell** est un programme qui fournit à l'utilisateur une interface pour interagir avec le système d'exploitation. Il permet de saisir des commandes et d'afficher les résultats en mode texte.

### Les différents shells sous Linux

- **Bash** (Bourne Again Shell) - Le plus répandu
- **Zsh** (Z Shell) - Avec de nombreuses améliorations
- **Fish** - Interface utilisateur avancée
- **Ksh** (Korn Shell)
- **Tcsh** (Enhanced C Shell)

> **Bash** est le shell par défaut sur la plupart des distributions Linux et sera utilisé dans ce cours.

## L'Invite de Commande (Prompt)

L'invite de commande affiche des informations contextuelles importantes :

```bash
utilisateur@nom_hôte[~]$ 
```

### Structure du prompt

- **utilisateur** : Nom de l'utilisateur connecté
- **nom_hôte** : Nom de la machine
- **~** : Répertoire courant (tilde = répertoire personnel)
- **$** ou **#** : Type de shell
  - `$` = utilisateur normal
  - `#` = super-utilisateur (root)

### Exemples pratiques

```bash
lordp@kali[~]$          # Utilisateur normal
root@kali[~]#           # Super-utilisateur
```

## Structure d'une Commande Linux

```bash
commande [options] [arguments]
```

### Composantes d'une commande

- **Commande** : Programme à exécuter
- **Options** : Modifient le comportement (`-l`, `--help`)
- **Arguments** : Données supplémentaires (fichiers, chemins)

### Exemple détaillé

```bash
lordp@kali[~]$ ls -l /home
```

- `ls` : Commande (list directory contents)
- `-l` : Option (format long)
- `/home` : Argument (répertoire à lister)

## Obtenir de l'Aide

### Commandes d'aide essentielles

```bash
# Aide rapide
lordp@kali[~]$ ls --help

# Manuel complet
lordp@kali[~]$ man ls

# Documentation info (alternative)
lordp@kali[~]$ info ls
```

## Système de Fichiers Linux

### Concepts fondamentaux

- **Fichier** : Collection de données avec un nom
- **Répertoire** : Fichier spécial pour organiser d'autres fichiers
- **Sensibilité à la casse** : `Fichier` ≠ `fichier`

### Bonnes pratiques de nommage

✅ **Recommandé** :
- Noms courts et significatifs
- Underscore `_` ou tiret `-`
- Sans accents ni caractères spéciaux

❌ **À éviter** :
- Espaces dans les noms
- Caractères spéciaux (`!`, `&`, `/`)
- Accents et cédilles

## Commandes Essentielles de Gestion de Fichiers

### Navigation

```bash
# Afficher le répertoire courant
lordp@kali[~]$ pwd

# Changer de répertoire
lordp@kali[~]$ cd /chemin/vers/dossier
lordp@kali[~]$ cd ..          # Répertoire parent
lordp@kali[~]$ cd ~           # Répertoire personnel
lordp@kali[~]$ cd -           # Répertoire précédent
```

### Création

```bash
# Créer un répertoire
lordp@kali[~]$ mkdir mon_dossier
lordp@kali[~]$ mkdir -p dossier/sous-dossier  # Créer l'arborescence

# Créer un fichier
lordp@kali[~]$ touch mon_fichier.txt
lordp@kali[~]$ echo "contenu" > fichier.txt
```

### Suppression

```bash
# Supprimer un fichier
lordp@kali[~]$ rm fichier.txt
lordp@kali[~]$ rm -i fichier.txt  # Confirmation

# Supprimer un répertoire vide
lordp@kali[~]$ rmdir dossier_vide

# Supprimer récursivement
lordp@kali[~]$ rm -r dossier
```

> ⚠️ **DANGER** : `rm -rf /` supprime TOUT le système !

### Copie et Déplacement

```bash
# Copier un fichier
lordp@kali[~]$ cp source.txt destination.txt
lordp@kali[~]$ cp -r dossier_source/ dossier_destination/

# Déplacer/renommer
lordp@kali[~]$ mv ancien_nom.txt nouveau_nom.txt
lordp@kali[~]$ mv fichier.txt /chemin/destination/
```

### Consultation

```bash
# Lister le contenu
lordp@kali[~]$ ls
lordp@kali[~]$ ls -l          # Format long
lordp@kali[~]$ ls -a          # Fichiers cachés
lordp@kali[~]$ ls -t          # Tri par date

# Afficher le contenu d'un fichier
lordp@kali[~]$ cat fichier.txt
```

## Gestion des Fichiers avec Espaces

### Problème courant

```bash
# ❌ NE PAS FAIRE
lordp@kali[~]$ cat note pour le cours
```

### Solutions

```bash
# ✅ UTILISER LES GUILLEMETS
lordp@kali[~]$ cat "note pour le cours"
lordp@kali[~]$ cat 'note pour le cours'

# ✅ OU ÉCHAPPER LES ESPACES
lordp@kali[~]$ cat note\ pour\ le\ cours
```

## Arborescence du Système Linux

### Structure principale

```
/
├── bin/      # Commandes essentielles
├── boot/     # Fichiers de démarrage
├── dev/      # Périphériques
├── etc/      # Configuration système
├── home/     # Répertoires utilisateurs
├── root/     # Home de root
├── lib/      # Bibliothèques système
├── mnt/      # Points de montage
├── opt/      # Logiciels optionnels
├── sbin/     # Commandes administrateur
├── tmp/      # Fichiers temporaires
├── usr/      # Applications utilisateur
└── var/      # Fichiers variables
```

### Types de chemins

- **Chemin absolu** : Commence par `/`
  ```bash
  lordp@kali[~]$ cd /home/lordp/documents
  ```

- **Chemin relatif** : Relatif au répertoire courant
  ```bash
  lordp@kali[~]$ cd documents/archives
  lordp@kali[~]$ cd ../../
  ```

## Recherche de Fichiers

### Commande `locate` (rapide)

```bash
# Recherche dans la base de données
lordp@kali[~]$ locate nom_fichier
lordp@kali[~]$ locate "*.txt"
```

### Commande `find` (puissante)

```bash
# Recherche récursive
lordp@kali[~]$ find /home -name "fichier.txt"
lordp@kali[~]$ find . -type f -name "*.conf"

# Filtres avancés
lordp@kali[~]$ find . -size +1M          # Fichiers > 1MB
lordp@kali[~]$ find . -user lordp        # Par propriétaire
lordp@kali[~]$ find . -executable        # Fichiers exécutables
lordp@kali[~]$ find . ! -executable      # Non exécutables
```

## Gestion des Permissions

### Comprendre les permissions

```bash
lordp@kali[~]$ ls -l
drwxr-xr-x 2 lordp lordp 4096 Dec 15 10:30 mon_dossier
-rw-r--r-- 1 lordp lordp  123 Dec 15 10:30 mon_fichier
```

### Structure des permissions

```
d r w x r - x r - x
│ │ │ │ │ │ │ │ │ │
│ │ │ │ │ │ │ │ │ └─ Exécution (autres)
│ │ │ │ │ │ │ │ └── Lecture (autres)
│ │ │ │ │ │ │ └──── Écriture (autres)
│ │ │ │ │ │ └───── Exécution (groupe)
│ │ │ │ │ └────── Lecture (groupe)
│ │ │ │ └─────── Écriture (groupe)
│ │ │ └──────── Exécution (propriétaire)
│ │ └───────── Lecture (propriétaire)
│ └────────── Écriture (propriétaire)
└──────────── Type (d=répertoire, -=fichier)
```

### Modification des permissions

#### Méthode symbolique

```bash
# Ajouter des permissions
lordp@kali[~]$ chmod u+x fichier        # Exécution propriétaire
lordp@kali[~]$ chmod g+w fichier        # Écriture groupe
lordp@kali[~]$ chmod o-r fichier        # Retirer lecture autres

# Combinaisons
lordp@kali[~]$ chmod u+rwx,g+rx,o+r fichier
```

#### Méthode octale

| Permission | Valeur |
|------------|--------|
| r (read)   | 4      |
| w (write)  | 2      |
| x (execute)| 1      |

```bash
# 755 = rwxr-xr-x
lordp@kali[~]$ chmod 755 fichier

# 644 = rw-r--r--
lordp@kali[~]$ chmod 644 fichier
```

### Modification de propriété

```bash
# Changer le propriétaire
lordp@kali[~]$ sudo chown nouvel_utilisateur fichier

# Changer le groupe
lordp@kali[~]$ sudo chgrp nouveau_groupe fichier

# Les deux en même temps
lordp@kali[~]$ sudo chown user:groupe fichier
```

## Bonnes Pratiques et Pièges à Éviter

### ✅ Bonnes habitudes

- Toujours tester les commandes destructives
- Utiliser `-i` pour les confirmations
- Sauvegarder régulièrement
- Documenter les opérations complexes

### ⚠️ Pièges courants

- Espaces dans les noms de fichiers
- Confusion entre `>` et `>>`
- Oubli des `sudo` nécessaires
- Mauvais chemins absolus/relatifs

## Exercices Pratiques

### Niveau Débutant

1. Créez une arborescence `projet/docs/archives`
2. Créez un fichier `readme.txt` avec votre nom
3. Listez le contenu avec détails
4. Copiez le fichier dans `archives/`
5. Supprimez le fichier original

### Niveau Intermédiaire

1. Trouvez tous les fichiers `.conf` dans `/etc`
2. Créez un fichier avec espaces et apprenez à le manipuler
3. Modifiez les permissions d'un fichier en `rw-r-----`
4. Changez le propriétaire d'un fichier de test

## Conclusion

Cette introduction couvre les bases essentielles pour travailler efficacement en ligne de commande Linux. La pratique régulière est clé pour maîtriser ces concepts.

**Prochaine étape** : Scripting Bash et automatisation des tâches !

<style>
/* Styles pour une meilleure présentation */
.post-content h1, .post-content h2, .post-content h3 {
  color: #0066ff;
  margin-top: 2rem;
  margin-bottom: 1rem;
}

.post-content code {
  background: #f8fafc;
  padding: 0.2rem 0.4rem;
  border-radius: 4px;
  border: 1px solid #e2e8f0;
  font-family: 'Courier New', monospace;
}

.post-content pre {
  background: #0f172a;
  color: #e2e8f0;
  padding: 1.5rem;
  border-radius: 8px;
  overflow-x: auto;
  margin: 1.5rem 0;
  border-left: 4px solid #0066ff;
}

.post-content blockquote {
  border-left: 4px solid #00c6ff;
  padding: 1rem 1.5rem;
  margin: 1.5rem 0;
  background: #f0f7ff;
  border-radius: 0 8px 8px 0;
}

.post-content table {
  width: 100%;
  border-collapse: collapse;
  margin: 1.5rem 0;
}

.post-content th, .post-content td {
  padding: 0.75rem;
  border: 1px solid #e2e8f0;
  text-align: left;
}

.post-content th {
  background: #0066ff;
  color: white;
}

.post-content tr:nth-child(even) {
  background: #f8fafc;
}
</style>
```

## 🎯 **Améliorations apportées :**

- ✅ **Structure claire** avec titres hiérarchisés
- ✅ **Code bien formaté** avec syntax highlighting
- ✅ **Exemples pratiques** et concrets
- ✅ **Avertissements visuels** pour les commandes dangereuses
- ✅ **Tableaux récapitulatifs** pour les permissions
- ✅ **Exercices progressifs** pour la pratique
- ✅ **Style cohérent** avec votre charte bleu cyber
- ✅ **Responsive** et lisible sur tous devices

Ce format est beaucoup plus professionnel et adapté à un cours en ligne ! 📚🐧
